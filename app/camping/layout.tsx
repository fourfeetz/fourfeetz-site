import { createPageMetadata } from "@/lib/seo";
export const metadata = createPageMetadata({ title: "Works Archive", description: "Explore FourFeetz Studios concepts, environments, and production references developed for original AI stories.", path: "/camping" });
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
