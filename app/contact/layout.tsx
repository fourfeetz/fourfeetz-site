import { createPageMetadata } from "@/lib/seo";
export const metadata = createPageMetadata({ title: "Contact", description: "Contact FourFeetz Studios about creative collaborations, AI film production, licensing, editorial inquiries, and partnerships.", path: "/contact" });
export default function Layout({ children }: Readonly<{ children: React.ReactNode }>) { return children; }
